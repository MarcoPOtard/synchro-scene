# Configuration du client (Vite) — développement local uniquement.
# En production, ne pas définir cette variable : le client se connecte
# automatiquement au serveur qui le sert (même origine).
VITE_SOCKET_URL=http://localhost:3001
